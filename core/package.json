{
  "name": "@helpdesk/core",
  "version": "1.0.0",
  "private": true,
  "description": "Validation schemas and types shared by client/ and server/",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "zod": "^4.4.3"
  }
}
